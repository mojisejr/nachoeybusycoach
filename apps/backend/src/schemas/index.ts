// Export all schemas for Sanity Studio
import { notification } from './notification';

// Schema types array for Sanity configuration
export const schemaTypes = [
  notification,
];

// Export individual schemas
export { notification };